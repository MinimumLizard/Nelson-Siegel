"""`python -m pipeline.update` — incremental version of backfill: fetch only
files that are not already recorded in the `files` table, parse the new ones.

STATUS: stub, for the same reason as backfill.py.
"""

import sys


def main() -> None:
    sys.exit("update: not implemented yet — waiting on sample-PDF inspection.")


if __name__ == "__main__":
    main()
