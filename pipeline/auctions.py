"""CBSL auction results ingestion — DELIBERATELY EMPTY.

A later stage will populate `observations` rows with source='auction' from
CBSL auction result publications. Stage 0 only reserves the module name and
the source label so the schema and downstream code don't need to change.
"""
