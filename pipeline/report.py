"""`python -m pipeline.report --isin LKB00934F154` — print and plot one bond's
yield and volume history from the database.

STATUS: stub until the database is being populated.
"""

import sys


def main() -> None:
    sys.exit("report: not implemented yet — waiting on sample-PDF inspection.")


if __name__ == "__main__":
    main()
